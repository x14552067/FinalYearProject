<%= bootstrap_form_with(model: @classgroup, local: true) do |form| %>
  <% if @classgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classgroup.errors.count, "error") %> prohibited this classgroup from being saved:</h2>

      <ul>
      <% @classgroup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="field">
      <%= form.text_field :class_name, label: "Class Name"%>
    </div>
    <div class="field">
      <%= form.text_field :course_name, label: "Course Name"%>
    </div>
    <div class="field">
      <%= form.text_field :class_description, label: "Class Description"%>
    </div>
  </div>

  <div class="actions">
    <%= form.submit "Create Class", class: "btn btn-primary"%>
  </div>
<% end %>
